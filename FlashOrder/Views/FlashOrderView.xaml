<UserControl x:Class="FlashOrder.Views.FlashOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlashOrder.Views"
             mc:Ignorable="d" 
                xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
                xmlns:prism="http://prismlibrary.com/"
               prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:flash="clr-namespace:Flash.Views;assembly=Flash"
             d:DesignHeight="450" d:DesignWidth="800"    >
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <prism:InvokeCommandAction Command="{Binding ExecuteLoadDelegateCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <!--<flash:FlashView Name="FlashView" x:FieldModifier="public"    ></flash:FlashView>-->
      
            <!--<ContentControl  prism:RegionManager.RegionName="Flash" ></ContentControl>-->
        <flash:FlashView  x:FieldModifier="public"  Name="FlashView" ></flash:FlashView>
  
    </Grid>
</UserControl>
