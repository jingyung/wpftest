<UserControl x:Class="FlashOrder.Views.FlashOrderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlashOrder"
             mc:Ignorable="d" 
                 xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid Name="dg" AutoGenerateColumns="False" ItemsSource="{Binding TickData}">
            <DataGrid.Columns>

                <DataGridTextColumn Header="Bid" Binding="{Binding Bid}" />
                <DataGridTextColumn Header="Price" Binding="{Binding Bid}" />
                <DataGridTextColumn Header="Ask" Binding="{Binding Ask}" />



            </DataGrid.Columns>

        </DataGrid>
    </Grid>
</UserControl>
